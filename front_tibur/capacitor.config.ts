import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'basics_tibur',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
